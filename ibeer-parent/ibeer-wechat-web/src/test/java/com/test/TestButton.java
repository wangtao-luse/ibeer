package com.test;


import org.json.JSONObject;

import com.ibeer.dto.menu.AbstractButton;
import com.ibeer.dto.menu.Button;

public class TestButton {
public static void main(String[] args) {
	Button button = new Button();
	button.getAbstractButton().add(new AbstractButton("菜单一"));
	button.getAbstractButton().add(new AbstractButton("菜单二"));
	button.getAbstractButton().add(new AbstractButton("菜单三"));
	 JSONObject.from
}
}
